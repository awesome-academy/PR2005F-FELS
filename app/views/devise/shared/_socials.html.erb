<div class="row" id="socialX">
	 <%- if devise_mapping.omniauthable? %>
		  <%- resource_class.omniauth_providers.each do |provider| %>
		    <%= link_to("Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider), class: "btn facebook-btn social-btn btn-block") %>
		    	<br />
		  <% end %>
		<% end %>
	<hr>
		 <%- if devise_mapping.omniauthable? %>
		  <%- resource_class.omniauth_providers.each do |provider| %>
		    <%= link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider), class: "btn google-btn social-btn btn-block" %><br />
		  <% end %>
		<% end %>
</div>

      

